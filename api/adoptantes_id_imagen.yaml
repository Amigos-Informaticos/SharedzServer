description: Sube una imagen al perfil de un adoptante
post:
  summary: Sube la imagen
  tags:
    - Adoptante
    - Imagenes
  operationId: imagenAdoptante
  parameters:
    - name: id_adoptante
      description: Id del adoptante cuya informacion se desea recuperar
      in: path
      schema:
        type: integer
        minimum: 1
        maximum: 999
      required: true
    - name: Token
      description: Token identificador del adoptante loggeado
      in: header
      schema:
        type: string
      required: true
  requestBody:
    content:
      multipart/form-data:
        schema:
          type: object
          properties:
            file:
              type: string
              format: binary
  responses:
    226:
      description: Se ha guardado la imagen de forma exitosa
    400:
      description: Peticion erronea o malformada
    401:
      description: Usuario no autenticado
    404:
      description: El adoptante indicado no se encuentra registrado
    419:
      description: La sesion del usuario ha expirado
